import React from 'react';
import ReactDOM from 'react-dom';
import DialogForm from "./DialogForm";

ReactDOM.render(
	<DialogForm itemId='scheduleShowing-1'/>,
	document.getElementById('v-dialog-form')
);